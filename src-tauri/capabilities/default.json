{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop notifications when the window is hidden, and the in-app update check.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "updater:default"
  ]
}

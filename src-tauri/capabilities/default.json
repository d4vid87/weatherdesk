{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop notifications when the window is hidden. Every platform, Android included.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
